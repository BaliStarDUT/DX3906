spring:
#  profiles:
#    active: test
  application:
    name: microservice-provider-dx3906    # 项目名称尽量用小写
  cloud:
    config:
      uri: http://windows.james.com:8040/
      profile: test                      # 指定profile，对应microservice-config-server所获取的配置文件中的{profile}
      label: master